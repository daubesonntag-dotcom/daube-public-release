name: Call Forge Cloudflare root purge

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/call-forge-cloudflare-root-purge.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  purge:
    uses: daubesonntag-dotcom/daube-forge-os/.github/workflows/cloudflare-root-purge-reusable.yml@main
