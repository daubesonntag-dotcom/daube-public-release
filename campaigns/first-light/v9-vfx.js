const CDN={motion:'https://cdn.jsdelivr.net/npm/motion@13.2.0/+esm',lenis:'https://cdn.jsdelivr.net/npm/lenis@1.3.26/+esm',ogl:'https://cdn.jsdelivr.net/npm/ogl@1.0.11/+esm',three:'https://cdn.jsdelivr.net/npm/three@0.174.0/+esm',wawa:'https://cdn.jsdelivr.net/npm/wawa-vfx-vanilla@1.2.10/+esm'};
async function safeImport(url){try{return await import(url)}catch(error){console.warn('[D’AUBE VFX] module fallback',url,error);return null}}
function canWebGL(){try{const c=document.createElement('canvas');return !!(c.getContext('webgl2')||c.getContext('webgl'))}catch{return false}}
function detectVfxTier(){const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,saveData=!!navigator.connection?.saveData,mem=Number(navigator.deviceMemory||4),cores=Number(navigator.hardwareConcurrency||4),small=innerWidth<680,webgl=canWebGL();if(reduced||saveData||!webgl||mem<=2||cores<=2)return'low';if(mem>=8&&cores>=8&&!small)return'high';return'medium'}
const tier=detectVfxTier();document.body.dataset.vfxTier=tier;document.body.classList.add('vfx-'+tier);const intro=document.getElementById('intro'),roseWrap=document.querySelector('.rose-image-wrap'),roseShot=document.querySelector('.rose-shot');let motionApi=null,lenisInstance=null;
async function initMotion(){if(tier==='low')return;motionApi=await safeImport(CDN.motion);if(!motionApi)return;const{animate,inView,stagger}=motionApi;if(roseWrap)animate(roseWrap,{opacity:[0,1],scale:[.965,1],y:[18,0]},{duration:1.2,ease:[.2,.8,.2,1]});const introCopy=document.querySelectorAll('.intro-copy > *');if(introCopy.length)animate(introCopy,{opacity:[0,1],y:[16,0]},{delay:stagger(.075),duration:.7,ease:[.25,.8,.2,1]});document.querySelectorAll('.section,.ceremony').forEach(el=>inView(el,()=>animate(el,{opacity:[.25,1],y:[24,0]},{duration:.85,ease:[.2,.75,.2,1]}),{margin:'-8% 0px -8% 0px',amount:.12}))}
async function initLenis(){if(tier==='low')return;const mod=await safeImport(CDN.lenis);if(!mod?.default)return;try{lenisInstance=new mod.default({lerp:tier==='high'?.075:.09,smoothWheel:true,wheelMultiplier:.88,touchMultiplier:1.05});const raf=t=>{lenisInstance?.raf(t);requestAnimationFrame(raf)};requestAnimationFrame(raf)}catch(error){console.warn('[D’AUBE VFX] Lenis fallback',error)}}
async function initOGL(){if(tier==='low')return false;const mod=await safeImport(CDN.ogl);if(!mod)return false;const canvas=document.getElementById('oglFx');try{const{Renderer,Program,Mesh,Triangle}=mod,renderer=new Renderer({canvas,alpha:true,dpr:Math.min(devicePixelRatio,tier==='high'?1.7:1.15)}),gl=renderer.gl;gl.clearColor(0,0,0,0);const geometry=new Triangle(gl),vertex=`attribute vec2 position;varying vec2 vUv;void main(){vUv=position*.5+.5;gl_Position=vec4(position,0.,1.);}`,fragment=`precision highp float;varying vec2 vUv;uniform float uTime;uniform vec2 uResolution;float hash(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453123);}float noise(vec2 p){vec2 i=floor(p),f=fract(p);f=f*f*(3.-2.*f);return mix(mix(hash(i),hash(i+vec2(1,0)),f.x),mix(hash(i+vec2(0,1)),hash(i+vec2(1,1)),f.x),f.y);}void main(){vec2 uv=vUv;float t=uTime*.11;float n=noise(uv*3.5+vec2(t,-t*.63));float n2=noise(uv*6.3+vec2(-t*.5,t*.8));vec2 c=uv-vec2(.66,.48);c.x*=uResolution.x/uResolution.y;float glow=exp(-dot(c,c)*3.6);vec3 gold=vec3(1.,.72,.39),rose=vec3(.72,.12,.25),col=mix(rose,gold,.45+.35*sin((uv.y+n*.16)*5.8+t));float a=(.055+n*.08+n2*.035)*glow+.016*n;gl_FragColor=vec4(col*a,a);}`,program=new Program(gl,{vertex,fragment,transparent:true,uniforms:{uTime:{value:0},uResolution:{value:[innerWidth,innerHeight]}}}),mesh=new Mesh(gl,{geometry,program});const resize=()=>{renderer.setSize(innerWidth,innerHeight);program.uniforms.uResolution.value=[innerWidth,innerHeight]};resize();addEventListener('resize',resize,{passive:true});let active=true;const loop=t=>{if(!active)return;program.uniforms.uTime.value=t*.001;renderer.render({scene:mesh});requestAnimationFrame(loop)};requestAnimationFrame(loop);intro.addEventListener('transitionend',e=>{if(e.propertyName==='opacity'&&intro.classList.contains('hidden'))active=false},{once:true});return true}catch(error){console.warn('[D’AUBE VFX] OGL fallback',error);return false}}
function initCanvasFallback(){const host=document.getElementById('threeFx');if(!host||tier==='low')return;const canvas=document.createElement('canvas'),ctx=canvas.getContext('2d');if(!ctx)return;host.appendChild(canvas);let w=0,h=0,particles=[],active=true;const count=tier==='high'?70:34,resize=()=>{const d=Math.min(devicePixelRatio,1.45);w=innerWidth;h=innerHeight;canvas.width=w*d;canvas.height=h*d;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(d,0,0,d,0,0)};resize();addEventListener('resize',resize,{passive:true});for(let i=0;i<count;i++)particles.push({x:Math.random()*w,y:Math.random()*h,r:.7+Math.random()*2.4,vx:(Math.random()-.5)*.12,vy:-.05-Math.random()*.21,a:.2+Math.random()*.7,p:Math.random()*6.28});const loop=t=>{if(!active)return;ctx.clearRect(0,0,w,h);for(const p of particles){p.x+=p.vx;p.y+=p.vy;p.p+=.018;if(p.y<-10){p.y=h+10;p.x=Math.random()*w}const pulse=.55+.45*Math.sin(p.p+t*.0018);ctx.beginPath();ctx.fillStyle=`rgba(255,228,183,${p.a*pulse})`;ctx.arc(p.x,p.y,p.r*pulse,0,Math.PI*2);ctx.fill()}requestAnimationFrame(loop)};requestAnimationFrame(loop);intro.addEventListener('transitionend',e=>{if(e.propertyName==='opacity'&&intro.classList.contains('hidden'))active=false},{once:true})}
async function initWawa(){if(tier!=='high')return false;const[THREE,VFX]=await Promise.all([safeImport(CDN.three),safeImport(CDN.wawa)]);if(!THREE||!VFX)return false;const host=document.getElementById('threeFx');try{const renderer=new THREE.WebGLRenderer({alpha:true,antialias:false,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.4));renderer.setSize(innerWidth,innerHeight);renderer.setClearColor(0,0);host.appendChild(renderer.domElement);const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(52,innerWidth/innerHeight,.1,100);camera.position.z=5;const particles=new VFX.VFXParticles('daubeRoseMagic',{nbParticles:1400,gravity:[0,-.03,0],renderMode:'billboard',intensity:1.35,appearance:VFX.AppearanceMode.Circular,easeFunction:'easeInOutSine',fadeAlpha:[.08,.9],fadeSize:[.05,.9],blendingMode:THREE.AdditiveBlending});scene.add(particles.getMesh());const emitter=new VFX.VFXEmitter('daubeRoseMagic',{loop:true,duration:3.4,nbParticles:18,spawnMode:'time',particlesLifetime:[1.5,3.8],startPositionMin:[-.72,-.85,-.2],startPositionMax:[.72,.95,.25],directionMin:[-.08,.05,-.02],directionMax:[.08,.22,.02],size:[.008,.032],speed:[.08,.32],colorStart:['#fff3d8','#ffd8a0','#f7b8c8'],colorEnd:['#d08a53','#b02d50','#fff2d3'],useLocalDirection:false});scene.add(emitter);const clock=new THREE.Clock();let active=true;const loop=()=>{if(!active)return;requestAnimationFrame(loop);const dt=clock.getDelta(),et=clock.getElapsedTime();emitter.update(et,dt);particles.update(et);renderer.render(scene,camera)};loop();const resize=()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)};addEventListener('resize',resize,{passive:true});intro.addEventListener('transitionend',e=>{if(e.propertyName==='opacity'&&intro.classList.contains('hidden')){active=false;particles.dispose?.();renderer.dispose()}},{once:true});return true}catch(error){console.warn('[D’AUBE VFX] wawa fallback',error);return false}}
function initParallax(){if(tier==='low'||!roseWrap)return;const max=tier==='high'?7:4;roseWrap.addEventListener('pointermove',e=>{const r=roseWrap.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;roseWrap.style.transform=`perspective(900px) rotateY(${x*max}deg) rotateX(${-y*max}deg)`},{passive:true});roseWrap.addEventListener('pointerleave',()=>roseWrap.style.transform='',{passive:true})}
function bindOpeningVfx(){['enterMusic','enterQuiet'].forEach(id=>document.getElementById(id)?.addEventListener('click',()=>{intro.classList.add('vfx-open');if(motionApi?.animate&&roseShot)motionApi.animate(roseShot,{scale:[1,1.035,1.018],filter:['saturate(1) brightness(1)','saturate(1.08) brightness(1.06)','saturate(1.04) brightness(1.015)']},{duration:1.45,ease:[.2,.8,.2,1]})},{capture:true}))}
await Promise.all([initMotion(),initLenis()]);const oglOK=await initOGL(),wawaOK=await initWawa();if(!wawaOK)initCanvasFallback();initParallax();bindOpeningVfx();console.info('[D’AUBE VFX]',{tier,ogl:oglOK,wawa:wawaOK,motion:!!motionApi,lenis:!!lenisInstance});