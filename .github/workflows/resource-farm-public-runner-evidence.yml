name: Resource Farm Public Runner Evidence

on:
  pull_request:
    paths:
      - '.github/workflows/resource-farm-public-runner-evidence.yml'
      - 'farm/evidence/**'
  push:
    branches: [main]
    paths:
      - '.github/workflows/resource-farm-public-runner-evidence.yml'
      - 'farm/evidence/**'
  schedule:
    - cron: '17 1 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prove-public-runner:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: CPU compute receipt
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import hashlib, json, os, platform, time
          started=time.time()
          digest=hashlib.sha256()
          for i in range(250000):
              digest.update(f'daube-farm-{i}'.encode())
          receipt={
            'schema':'daube.resource-farm-ci-compute-receipt.v1',
            'resourceClasses':['compute','ci-build'],
            'backend':'github-hosted-linux-cpu',
            'runnerOS':platform.system(),
            'arch':platform.machine(),
            'cpuCount':os.cpu_count(),
            'sha256':digest.hexdigest(),
            'latencyMs':round((time.time()-started)*1000,2),
            'paidSpendAuthorized':False,
            'privateAssetsUsed':False,
          }
          print(json.dumps(receipt, sort_keys=True))
          assert receipt['runnerOS']=='Linux'
          assert receipt['cpuCount'] and receipt['cpuCount'] >= 1
          PY

      - name: Compiler and executable receipt
        shell: bash
        run: |
          set -euo pipefail
          cat >/tmp/farm_canary.c <<'C'
          #include <stdio.h>
          int main(void){ unsigned long x=2166136261u; for(unsigned long i=0;i<100000;i++){x^=i;x*=16777619u;} printf("FARM_COMPILED_OK:%lu\n",x); return 0; }
          C
          gcc -O2 /tmp/farm_canary.c -o /tmp/farm_canary
          /tmp/farm_canary | tee /tmp/farm_compiler_receipt.txt
          grep -q '^FARM_COMPILED_OK:' /tmp/farm_compiler_receipt.txt

      - name: Headless browser receipt
        shell: bash
        run: |
          set -euo pipefail
          CHROME="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser || true)"
          test -n "$CHROME"
          cat >/tmp/farm-browser.html <<'HTML'
          <!doctype html><html><body><div id="result">boot</div><script>
          const values=new Uint32Array(4096); for(let i=0;i<values.length;i++) values[i]=(i*2654435761)>>>0;
          let x=0; for(const v of values) x=(x^v)>>>0;
          document.getElementById('result').textContent='FARM_BROWSER_OK:'+navigator.userAgent+':'+x;
          </script></body></html>
          HTML
          python3 -m http.server 8765 --directory /tmp >/tmp/farm-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID || true' EXIT
          sleep 1
          "$CHROME" --headless --no-sandbox --disable-gpu --dump-dom http://127.0.0.1:8765/farm-browser.html | tee /tmp/farm-browser-receipt.html
          grep -q 'FARM_BROWSER_OK:' /tmp/farm-browser-receipt.html
          echo "browser_binary=$CHROME"
