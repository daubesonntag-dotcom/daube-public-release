#version 450
layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
layout(set = 0, binding = 0, std430) buffer OutputBuffer {
  uint values[];
} outbuf;

void main() {
  uint i = gl_GlobalInvocationID.x;
  if (i < 4u) {
    outbuf.values[i] = 0xDA00005Au + (i * 0x00000101u);
  }
}
